---
title: "JavaScript worker pools"
description: "A zero-dependency worker pool for running CPU-heavy JavaScript off the main thread in Node.js, Deno, and Bun."
template: splash
tableOfContents: false
prev: false
next: false
head:
  - tag: meta
    attrs:
      property: og:type
      content: website
  - tag: meta
    attrs:
      property: og:title
      content: "Knitting — Run CPU-heavy JavaScript off the main thread"
  - tag: meta
    attrs:
      property: og:description
      content: "A zero-dependency worker pool for running CPU-heavy JavaScript off the main thread in Node.js, Deno, and Bun."
  - tag: meta
    attrs:
      name: twitter:title
      content: "Knitting — Run CPU-heavy JavaScript off the main thread"
  - tag: meta
    attrs:
      name: twitter:description
      content: "A zero-dependency worker pool for running CPU-heavy JavaScript off the main thread in Node.js, Deno, and Bun."
  - tag: meta
    attrs:
      name: robots
      content: "index,follow,max-image-preview:large"
---

import PoolCycle from "../../components/PoolCycle.astro";
import { Code } from "@astrojs/starlight/components";

export const quickStart = `import { createPool, isMain } from "knitting";

export const square = (value: number) => value * value;
export const greet = (name: string) => \`hello \${name}\`;

if (isMain) {
  using pool = createPool({ threads: 2 })({ square, greet });

  const [four, message] = await Promise.all([
    pool.call.square(2),
    pool.call.greet("knitting"),
  ]);

  console.log({ four, message });
}`;

export const install = `npm install knitting
deno add --npm knitting`;

<div data-knit-home-marker hidden></div>

<section class="kh-hero" aria-labelledby="kh-hero-title">
  <div class="kh-hero-grid">
    <div class="kh-hero-copy" data-reveal>
      <h1 id="kh-hero-title">
        Move JavaScript<br />
        <span>off the main thread.</span>
      </h1>
    </div>

    <div class="kh-hero-visual" data-reveal>
      <PoolCycle caption="One call site, whether work runs in a thread or an isolated process." />
    </div>

    <div class="kh-hero-bottom" data-reveal>
      <div class="kh-actions">
        <a class="kh-button kh-button--primary kh-button--cloud" href="/start/quick-start/">
          <img
            class="kh-cloud"
            src={`${import.meta.env.BASE_URL}brand/wool-cloud.png`}
            alt=""
            aria-hidden="true"
            width="1200"
            height="457"
          />
          <span class="kh-cloud-label">Get started <span aria-hidden="true">→</span></span>
        </a>
        <a class="kh-button kh-button--secondary" href="https://github.com/mimiMonads/knitting">
          View on GitHub
        </a>
      </div>

      <div class="kh-install-line" aria-label="Install Knitting with npm">
        <span aria-hidden="true">$</span>
        <code>npm install knitting</code>
        <span>npm + JSR</span>
      </div>
    </div>

  </div>
</section>

<section class="kh-proof" aria-label="Project highlights" data-reveal>
  <div>
    <strong>0</strong>
    <span>runtime dependencies</span>
  </div>
  <div>
    <strong>1</strong>
    <span>typed API</span>
  </div>
  <div>
    <strong>2</strong>
    <span>worker boundaries</span>
  </div>
  <div>
    <strong>3</strong>
    <span>JavaScript runtimes</span>
  </div>
</section>

<div class="kh-main">
  <section class="kh-section kh-start" data-reveal aria-labelledby="kh-start-title">
    <div class="kh-section-intro kh-section-intro--center">
      <p class="kh-kicker">A simpler worker API</p>
      <h2 id="kh-start-title">From an exported function to a worker call.</h2>
      <p>
        No worker entry point, message router, or response plumbing. Define tasks
        at the module level, pass them to a pool, and keep the call site typed.
      </p>
    </div>

    <div class="kh-start-grid">
      <div class="kh-code-panel">
        <div class="kh-code-panel-head">
          <span>main.ts</span>
          <span>TypeScript</span>
        </div>
        <div class="kh-highlighted-code">
          <Code code={quickStart} lang="ts" />
        </div>
      </div>

      <ol class="kh-steps">
        <li>
          <span>01</span>
          <div>
            <strong>Export the task</strong>
            <p>Plain functions are tasks. Add <code>task()</code> only when you need options.</p>
          </div>
        </li>
        <li>
          <span>02</span>
          <div>
            <strong>Create the pool</strong>
            <p>Choose threads for speed or processes for stronger isolation.</p>
          </div>
        </li>
        <li>
          <span>03</span>
          <div>
            <strong>Call it like any function</strong>
            <p>The result is a promise. Errors come back as standard <code>Error</code> objects.</p>
          </div>
        </li>
      </ol>
    </div>

    <div class="kh-install-box">
      <div>
        <span>Install</span>
        <strong>Ready in one dependency.</strong>
      </div>
      <pre class="kh-install-code"><code>{install}</code></pre>
      <a href="/start/installation/">Installation details <span aria-hidden="true">→</span></a>
    </div>
  </section>

  <section class="kh-section kh-features" data-reveal aria-labelledby="kh-features-title">
    <img
      class="kh-mascot-watermark"
      src={`${import.meta.env.BASE_URL}brand/knitting-mascot.png`}
      alt=""
      aria-hidden="true"
      loading="lazy"
      decoding="async"
      width="1024"
      height="1024"
    />
    <div class="kh-section-intro">
      <p class="kh-kicker">The mechanics around the call</p>
      <h2 id="kh-features-title">The boundary is part of the API.</h2>
      <p>
        Knitting handles the mechanics that make workers useful in production:
        moving data, isolating code, scheduling work, and preserving stack traces
        and error causes.
      </p>
    </div>

    <div class="kh-feature-grid kh-grid">
      <article class="kh-feature kh-feature--wide">
        <div class="kh-feature-icon" aria-hidden="true">↔</div>
        <p class="kh-feature-label">Fast by construction</p>
        <h3>Move the work, not all the bytes.</h3>
        <p>
          Pass typed payloads and promises. Move large buffers through zero-copy
          handles. Set timeouts, cancel with abort signals, and keep stack traces
          and error causes intact.
        </p>
        <ul class="kh-chip-list" aria-label="Call and payload features">
          <li>Promise inputs</li>
          <li>SharedArrayBuffer</li>
          <li>ProcessSharedBuffer</li>
          <li>BufferReference</li>
          <li>Task timeouts</li>
          <li>Abort signals</li>
          <li>Typed arrays</li>
          <li>Error cause chains</li>
        </ul>
        <a href="/guides/payloads/">Explore payloads <span aria-hidden="true">→</span></a>
      </article>

      <article class="kh-feature kh-feature--dark">
        <div class="kh-feature-icon" aria-hidden="true">⌁</div>
        <p class="kh-feature-label">Choose your boundary</p>
        <h3>Use threads for trusted work. Use processes for isolation.</h3>
        <p>
          The same task API works with runtime threads or isolated processes.
          Combine <code>importTask</code> with permissions, bubblewrap, or a container.
        </p>
        <div class="kh-boundary-dial" aria-label="Isolation options from thread to sandbox">
          <span>thread</span><i></i><span>process</span><i></i><span>sandbox</span>
        </div>
        <a href="/guides/process-workers/">Choose a boundary <span aria-hidden="true">→</span></a>
      </article>

      <article class="kh-feature">
        <div class="kh-feature-icon" aria-hidden="true">✓</div>
        <p class="kh-feature-label">Failure is part of the API</p>
        <h3>Deadlines, cancellation, and clear errors.</h3>
        <p>
          Limit cooperative work with timeouts and abort signals, or hard-kill
          runaway CPU work after a deadline. Stack traces and error causes come back intact.
        </p>
        <a href="/guides/defining-tasks/">Define resilient tasks <span aria-hidden="true">→</span></a>
      </article>

      <article class="kh-feature">
        <div class="kh-feature-icon" aria-hidden="true">◎</div>
        <p class="kh-feature-label">Observable on demand</p>
        <h3>Quiet by default. Detailed when debugging.</h3>
        <p>
          Enable only the diagnostics you need: host setup, imports, lifecycle,
          signals, or global state. When disabled, the logger is not imported.
        </p>
        <a href="/guides/creating-pools/">Configure a pool <span aria-hidden="true">→</span></a>
      </article>
    </div>
  </section>

  <div class="kh-main-rule" aria-hidden="true"></div>

  <section id="benchmarks" class="kh-benchmark" data-reveal aria-labelledby="kh-benchmark-title">
    <div class="kh-benchmark-head">
      <div>
        <p class="kh-kicker">Measured in practice</p>
        <h2 id="kh-benchmark-title">Low-latency IPC.</h2>
      </div>
      <p>
        These measurements come from the public benchmark suite. Review the
        methodology, hardware, and raw results before comparing workloads.
      </p>
    </div>

    <div class="kh-metric-grid kh-grid">
      <article>
        <span>Scheduling roundtrip</span>
        <strong>538<small>ns</small></strong>
        <p>Node · 53.8 μs per batch of 100</p>
      </article>
      <article>
        <span>Zero-copy roundtrip</span>
        <strong>816<small>ns</small></strong>
        <p>Node · 81.6 μs per batch of 100</p>
      </article>
      <article>
        <span>1 MiB copied round-trip</span>
        <strong>313<small>μs</small></strong>
        <p>Bun · 31.3 ms per batch of 100</p>
      </article>
    </div>

    <div class="kh-benchmark-links">
      <a class="kh-button kh-button--primary" href="/benchmarks/introduction/">
        Read the benchmarks <span aria-hidden="true">→</span>
      </a>
      <a href="https://github.com/mimiMonads/knitting-vs-tokio-bench">Open the source suite →</a>
    </div>
  </section>

  <section class="kh-section" data-reveal aria-labelledby="kh-use-title">
    <div class="kh-section-intro kh-section-intro--split">
      <div>
        <p class="kh-kicker">Keep the function. Move the heavy work.</p>
        <h2 id="kh-use-title">Keep CPU-heavy work off the event loop.</h2>
      </div>
      <p>
        Knitting is the middle ground between running CPU-heavy work on the
        request thread and moving it to a separate service. Keep the work in your
        application without blocking the event loop.
      </p>
    </div>

    <div class="kh-use-grid kh-grid">
      <a href="/examples/data_transforms/rendering_output/react_ssr/">
        <span>Rendering</span>
        <strong>React SSR</strong>
        <p>Render components away from the request thread.</p>
        <i aria-hidden="true">→</i>
      </a>
      <a href="/examples/data_transforms/validation/jwt_revalidation/">
        <span>Security</span>
        <strong>JWT revalidation</strong>
        <p>Verify and reissue tokens with Web Crypto.</p>
        <i aria-hidden="true">→</i>
      </a>
      <a href="/examples/data_transforms/validation/salt_hashing/">
        <span>Cryptography</span>
        <strong>Password hashing</strong>
        <p>Run PBKDF2 without stalling other requests.</p>
        <i aria-hidden="true">→</i>
      </a>
      <a href="/examples/data_transforms/rendering_output/markdown_to_html/">
        <span>Pipelines</span>
        <strong>Markdown + Brotli</strong>
        <p>Transform and compress in the same worker call.</p>
        <i aria-hidden="true">→</i>
      </a>
    </div>
  </section>

  <section class="kh-final" data-reveal aria-labelledby="kh-final-title">
    <p class="kh-kicker">Your hot path can stay a function.</p>
    <h2 id="kh-final-title">Give it a better place to run.</h2>
    <p>Start with one exported task. Add stronger isolation only when you need it.</p>
    <div class="kh-actions">
      <a class="kh-button kh-button--primary" href="/start/quick-start/">
        Create your first pool <span aria-hidden="true">→</span>
      </a>
      <a class="kh-button kh-button--secondary" href="/extras/why/">Why Knitting?</a>
    </div>
  </section>
</div>

<section hidden aria-label="Notes for AI agents">
  <p>
    Knitting ships machine-readable docs at <a href="/llms.txt">/llms.txt</a>
    and <a href="/llms-full.txt">/llms-full.txt</a>. Read them before generating code.
  </p>
  <p>
    Workers re-import the task module, including its top-level imports. Keep task
    modules lean, export every task, and use <code>importTask</code> when code
    should load only inside the worker.
  </p>
</section>

<style is:global>{`
html:has([data-knit-home-marker]) {
  /* Three-color system. Supporting tones are derived, never introduced ad hoc. */
  --kh-night: #160c08;
  --kh-paper: #fff1d6;
  --kh-accent: #ff7a1f;
  --kh-surface: color-mix(in srgb, var(--kh-night) 88%, var(--kh-paper));
  --kh-muted: color-mix(in srgb, var(--kh-paper) 72%, var(--kh-night));
  --kh-ink: var(--kh-paper);
  --kh-card: var(--kh-surface);
  --kh-code: var(--kh-night);
  --kh-code-ink: var(--kh-paper);
  --kh-code-muted: var(--kh-muted);
  --kh-button-ink: var(--kh-night);
  --kh-cloud-ink: var(--kh-night);
  --kh-line: color-mix(in srgb, var(--kh-muted) 24%, transparent);
  --kh-night-line: var(--kh-line);
  overflow-x: clip;
  background: var(--kh-night);
}

html[data-theme="light"]:has([data-knit-home-marker]) {
  --kh-night: #fff8ee;
  --kh-paper: #2b1d14;
  --kh-accent: #b45216;
  --kh-surface: #f5e3cf;
  --kh-muted: #705343;
  --kh-ink: var(--kh-paper);
  --kh-card: #fffaf3;
  --kh-code: #f5e3cf;
  --kh-code-ink: var(--kh-paper);
  --kh-code-muted: #8a6852;
  --kh-button-ink: #fff1d6;
  --kh-cloud-ink: var(--kh-paper);
  --kh-line: color-mix(in srgb, var(--kh-ink) 18%, transparent);
  --kh-night-line: var(--kh-line);
}

html:has([data-knit-home-marker]) body,
html:has([data-knit-home-marker]) main {
  background: var(--kh-night);
}

html:has([data-knit-home-marker]) .header {
  border-color: transparent;
  background: var(--kh-night);
  color: var(--kh-paper);
}

html:has([data-knit-home-marker]) .header .site-title,
html:has([data-knit-home-marker]) .header a,
html:has([data-knit-home-marker]) .header select {
  color: var(--kh-paper);
}

html:has([data-knit-home-marker]) .header site-search > button {
  border-color: var(--kh-line);
  background: var(--kh-night);
  color: var(--kh-muted);
}

@media (min-width: 50rem) {
  html:has([data-knit-home-marker]) {
    --sl-nav-height: 5rem;
  }

  html:has([data-knit-home-marker]) body > .page > header.header {
    padding-inline: 0;
  }

  html:has([data-knit-home-marker]) body > .page > header.header > .header {
    position: relative;
    width: min(100% - 3rem, 66rem);
    display: flex;
    justify-content: space-between;
    margin-inline: auto;
  }

  html:has([data-knit-home-marker]) body > .page > header.header > .header > div:nth-child(2) {
    position: absolute;
    left: 50%;
    width: min(25vw, 19rem);
    transform: translateX(-50%);
  }

  html:has([data-knit-home-marker]) .header site-search > button {
    width: 100%;
    max-width: none;
  }
}

html:has([data-knit-home-marker]) main {
  padding-bottom: 0;
}

html:has([data-knit-home-marker]) .content-panel,
html:has([data-knit-home-marker]) .sl-container {
  max-width: none;
  padding-inline: 0;
}

html:has([data-knit-home-marker]) .sl-markdown-content {
  max-width: none;
}

html:has([data-knit-home-marker]) .sl-markdown-content > * + * {
  margin-top: 0;
}

html:has([data-knit-home-marker]) main > .content-panel:has(> .sl-container > h1#_top) {
  display: none;
}

html:has([data-knit-home-marker]) .kh-hero *,
html:has([data-knit-home-marker]) .kh-proof *,
html:has([data-knit-home-marker]) .kh-main * {
  box-sizing: border-box;
}

html:has([data-knit-home-marker]) .kh-hero {
  position: relative;
  isolation: isolate;
  overflow: hidden;
  background: var(--kh-night);
  color: var(--kh-paper);
}

html:has([data-knit-home-marker]) .kh-hero::before {
  content: none;
}

html:has([data-knit-home-marker]) .kh-hero::after {
  content: none;
}

html:has([data-knit-home-marker]) .kh-hero-grid,
html:has([data-knit-home-marker]) .kh-proof,
html:has([data-knit-home-marker]) .kh-section,
html:has([data-knit-home-marker]) .kh-benchmark,
html:has([data-knit-home-marker]) .kh-final {
  width: min(100% - 3rem, 76rem);
  margin-inline: auto;
}

html:has([data-knit-home-marker]) .kh-hero-grid {
  position: relative;
  z-index: 1;
  width: min(100% - 3rem, 66rem);
  min-height: 0;
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  align-items: center;
  gap: clamp(3.25rem, 5vw, 4rem);
  padding-block: clamp(2.5rem, 4vw, 3rem) clamp(3rem, 5vw, 4.5rem);
}

html:has([data-knit-home-marker]) .kh-hero-copy {
  max-width: 64rem;
  min-width: 0;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: clamp(1rem, 2vw, 1.25rem);
}

html:has([data-knit-home-marker]) .kh-release {
  display: inline-flex;
  align-items: center;
  gap: 0.65rem;
  padding: 0.3rem 0.75rem 0.3rem 0.32rem;
  border: 1px solid color-mix(in srgb, var(--kh-accent) 38%, transparent);
  border-radius: 999px;
  color: var(--kh-muted);
  font-size: 0.78rem;
  font-weight: 650;
  text-decoration: none;
  transition: border-color 160ms ease, color 160ms ease;
}

html:has([data-knit-home-marker]) .kh-release:hover {
  border-color: var(--kh-accent);
  color: var(--kh-paper);
}

html:has([data-knit-home-marker]) .kh-release span:first-child {
  padding: 0.28rem 0.55rem;
  border-radius: 999px;
  background: var(--kh-accent);
  color: var(--kh-button-ink);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
  letter-spacing: -0.02em;
}

html:has([data-knit-home-marker]) .kh-hero h1 {
  margin: 0;
  color: var(--kh-paper);
  font-size: clamp(3.25rem, 6vw, 5.65rem);
  font-weight: 720;
  line-height: 1.2;
  letter-spacing: -0.06em;
  text-wrap: balance;
}

html:has([data-knit-home-marker]) .kh-hero h1 span {
  background: linear-gradient(100deg, var(--kh-paper), var(--kh-accent));
  background-clip: text;
  color: transparent;
}

html:has([data-knit-home-marker]) .kh-hero-lede {
  max-width: 38rem;
  margin: 0;
  color: var(--kh-muted);
  font-size: clamp(1.05rem, 1.8vw, 1.22rem);
  line-height: 1.65;
}

html:has([data-knit-home-marker]) .kh-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

html:has([data-knit-home-marker]) .kh-hero-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem 2rem;
}

html:has([data-knit-home-marker]) .kh-button {
  min-height: 3rem;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.7rem;
  padding: 0.72rem 1.2rem;
  border: 1px solid var(--kh-line);
  border-radius: 999px;
  color: var(--kh-paper);
  font-size: 0.92rem;
  font-weight: 720;
  line-height: 1;
  text-decoration: none;
  transition: transform 160ms ease, border-color 160ms ease, background 160ms ease;
}

html:has([data-knit-home-marker]) .kh-button:hover {
  transform: translateY(-1px);
  border-color: var(--kh-accent);
}

html:has([data-knit-home-marker]) .kh-button--primary {
  border-color: var(--kh-accent);
  background: var(--kh-accent);
  color: var(--kh-button-ink);
  box-shadow: 0 0 2.5rem color-mix(in srgb, var(--kh-accent) 13%, transparent);
}

html:has([data-knit-home-marker]) .kh-button--primary:hover {
  background: color-mix(in srgb, var(--kh-accent) 82%, var(--kh-paper));
}

html:has([data-knit-home-marker]) .kh-hero .kh-button--cloud {
  position: relative;
  isolation: isolate;
  width: 13.75rem;
  min-height: 5.25rem;
  padding: 0.9rem 2rem 0.72rem;
  border: 0;
  border-radius: 0;
  overflow: visible;
  background: none;
  box-shadow: none;
  color: var(--kh-cloud-ink);
}

html:has([data-knit-home-marker]) .kh-button--cloud .kh-cloud {
  position: absolute;
  z-index: -1;
  inset: 0;
  width: 100%;
  height: 100%;
  margin: 0;
  object-fit: fill;
  overflow: visible;
  filter: drop-shadow(0 0.45rem 0.8rem color-mix(in srgb, var(--kh-night) 34%, transparent));
  transition: transform 180ms ease, filter 180ms ease;
}

html:has([data-knit-home-marker]) .kh-cloud-label {
  position: relative;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  gap: 0.6rem;
  transform: rotate(-1deg);
}

html:has([data-knit-home-marker]) .kh-hero .kh-button--cloud:hover {
  border-color: transparent;
  background: none;
}

html:has([data-knit-home-marker]) .kh-button--cloud:hover .kh-cloud {
  transform: translateY(-0.12rem) rotate(-1deg) scale(1.02);
  filter: drop-shadow(0 0.65rem 1rem color-mix(in srgb, var(--kh-night) 42%, transparent));
}

html:has([data-knit-home-marker]) .kh-button--secondary {
  background: color-mix(in srgb, var(--kh-surface) 64%, transparent);
}

html:has([data-knit-home-marker]) .kh-install-line {
  width: min(100%, 31rem);
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  gap: 0.7rem;
  padding: 0.72rem 0.9rem;
  border: 1px solid var(--kh-line);
  border-radius: 0.65rem;
  background: color-mix(in srgb, var(--kh-night) 58%, transparent);
  color: var(--kh-accent);
  box-shadow: inset 0 1px 0 color-mix(in srgb, var(--kh-paper) 4%, transparent);
}

html:has([data-knit-home-marker]) .kh-install-line code {
  padding: 0;
  border: 0;
  background: none;
  color: var(--kh-paper);
  font-size: 0.82rem;
}

html:has([data-knit-home-marker]) .kh-install-line > span:last-child {
  color: var(--kh-muted);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
}

/* Reintroduced pool hero, reduced to the same five-color system. */
html:has([data-knit-home-marker]) .kh-hero-visual {
  min-width: 0;
  overflow: visible;
  border: 0;
  background: none;
  box-shadow: none;
  backdrop-filter: none;
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc {
  --pc-ink: var(--kh-paper);
  --pc-dim: var(--kh-muted);
  --pc-line: var(--kh-line);
  --pc-bg: var(--kh-night);
  --pc-chrome: var(--kh-surface);
  --pc-accent: var(--kh-accent);
  --pc-key: var(--kh-paper);
  --pc-str: var(--kh-accent);
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-win {
  border-color: color-mix(in srgb, var(--kh-paper) 68%, transparent);
  border-radius: 0.9rem;
  box-shadow: none;
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-bar {
  position: relative;
  z-index: 2;
  height: 0;
  border: 0;
  background: none;
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-lights,
html:has([data-knit-home-marker]) .kh-hero-visual .pc-name {
  display: none;
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-pane--code {
  border-right-color: color-mix(in srgb, var(--kh-paper) 68%, transparent);
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc figcaption {
  display: none;
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-foot {
  border-top-color: color-mix(in srgb, var(--kh-paper) 68%, transparent);
  background: transparent;
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-lights i:nth-child(1) {
  background: var(--kh-muted);
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-lights i:nth-child(2) {
  background: var(--kh-paper);
}

html:has([data-knit-home-marker]) .kh-hero-visual .pc-lights i:nth-child(3) {
  background: var(--kh-accent);
}

html:has([data-knit-home-marker]) .kh-proof {
  width: min(100% - 3rem, 66rem);
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  border-top: 1px solid var(--kh-line);
  border-left: 1px solid var(--kh-line);
}

html:has([data-knit-home-marker]) .kh-proof > div {
  min-width: 0;
  display: flex;
  align-items: baseline;
  gap: 0.7rem;
  margin-top: 0;
  padding: 1.3rem 1.4rem;
  border-right: 1px solid var(--kh-line);
  border-bottom: 1px solid var(--kh-line);
  background: var(--kh-night);
}

html:has([data-knit-home-marker]) .kh-proof strong {
  color: var(--kh-ink);
  font-size: 1.7rem;
  font-variant-numeric: tabular-nums;
  line-height: 1;
}

html:has([data-knit-home-marker]) .kh-proof span {
  color: var(--kh-muted);
  font-size: 0.75rem;
  line-height: 1.3;
}

html:has([data-knit-home-marker]) .kh-main {
  color: var(--kh-ink);
}

/* Starlight adds a one-rem rhythm to generic markdown siblings. Reset it
   where the homepage owns the grid rhythm so separators stay one pixel wide. */
html:has([data-knit-home-marker]) .kh-main > .kh-section,
html:has([data-knit-home-marker]) .kh-main > .kh-benchmark,
html:has([data-knit-home-marker]) .kh-main > .kh-final,
html:has([data-knit-home-marker]) .kh-features > .kh-section-intro,
html:has([data-knit-home-marker]) .kh-start-grid,
html:has([data-knit-home-marker]) .kh-feature-grid,
html:has([data-knit-home-marker]) .kh-metric-grid,
html:has([data-knit-home-marker]) .kh-use-grid {
  margin-top: 0;
}

html:has([data-knit-home-marker]) .kh-feature-grid > .kh-feature,
html:has([data-knit-home-marker]) .kh-metric-grid > article {
  margin-top: 0;
}

html:has([data-knit-home-marker]) .kh-start {
  width: min(100% - 3rem, 66rem);
}

html:has([data-knit-home-marker]) .kh-section {
  padding-block: clamp(4.5rem, 8vw, 7rem);
}

/* A contained Bun-style rule gives the main page a deliberate landmark without
   letting a decorative layer bleed into either neighboring section. */
html:has([data-knit-home-marker]) .kh-main-rule {
  width: min(100% - 3rem, 66rem);
  height: 9px;
  margin: 0 auto;
  background:
    linear-gradient(var(--kh-line) 0 0) left bottom / 100% 1px no-repeat,
    repeating-linear-gradient(
      to right,
      var(--kh-line) 0 1px,
      transparent 1px 5rem
    ) left bottom / 100% 9px no-repeat,
    repeating-linear-gradient(
      to right,
      color-mix(in srgb, var(--kh-line) 58%, transparent) 0 1px,
      transparent 1px 0.5rem
    ) left bottom / 100% 4px no-repeat;
  pointer-events: none;
}

html:has([data-knit-home-marker]) .kh-features {
  position: relative;
  isolation: isolate;
  --kh-mascot-size: clamp(18rem, 35vw, 30rem);
  --kh-mascot-right: clamp(0rem, 2vw, 2rem);
}

html:has([data-knit-home-marker]) .kh-features::before {
  content: "";
  position: absolute;
  z-index: -1;
  width: min(44rem, 72vw);
  height: min(44rem, 72vw);
  top: -8rem;
  right: clamp(-10rem, -7vw, -3rem);
  border-radius: 50%;
  background: radial-gradient(
    circle,
    color-mix(in srgb, var(--kh-accent) 13%, transparent),
    transparent 68%
  );
  pointer-events: none;
}

html:has([data-knit-home-marker]) .kh-mascot-watermark {
  position: absolute;
  z-index: -1;
  width: var(--kh-mascot-size);
  height: auto;
  top: clamp(1.5rem, 4vw, 3.5rem);
  right: var(--kh-mascot-right);
  margin: 0;
  opacity: 0.12;
  transform: rotate(4deg);
  filter: saturate(0.82) contrast(1.04);
  mask-image: linear-gradient(to bottom, black 50%, transparent 100%);
  pointer-events: none;
  user-select: none;
}

html:has([data-knit-home-marker]) .kh-features > :not(.kh-mascot-watermark) {
  position: relative;
  z-index: 1;
}

html:has([data-knit-home-marker]) .kh-section-intro {
  max-width: 50rem;
  display: grid;
  gap: 1rem;
  margin-bottom: clamp(2.25rem, 4vw, 3.5rem);
}

html:has([data-knit-home-marker]) .kh-section-intro--center {
  margin-inline: auto;
  text-align: center;
}

html:has([data-knit-home-marker]) .kh-section-intro--split {
  max-width: none;
  grid-template-columns: minmax(0, 1.1fr) minmax(18rem, 0.7fr);
  align-items: end;
  gap: 3rem;
}

html:has([data-knit-home-marker]) .kh-section-intro--split > div {
  display: grid;
  gap: 1rem;
}

html:has([data-knit-home-marker]) .kh-kicker,
html:has([data-knit-home-marker]) .kh-feature-label {
  margin: 0;
  color: var(--kh-accent);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
  font-weight: 760;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

html:has([data-knit-home-marker]) .kh-main h2,
html:has([data-knit-home-marker]) .kh-main h3,
html:has([data-knit-home-marker]) .kh-main p {
  margin: 0;
}

html:has([data-knit-home-marker]) .kh-main h2 {
  color: var(--kh-ink);
  font-size: clamp(2.5rem, 5vw, 4.4rem);
  font-weight: 700;
  line-height: 1;
  letter-spacing: -0.055em;
  text-wrap: balance;
}

html:has([data-knit-home-marker]) .kh-main h3 {
  color: var(--kh-ink);
  font-size: clamp(1.45rem, 2.4vw, 2rem);
  line-height: 1.12;
  letter-spacing: -0.035em;
  text-wrap: balance;
}

html:has([data-knit-home-marker]) .kh-section-intro > p:not(.kh-kicker),
html:has([data-knit-home-marker]) .kh-section-intro--split > p {
  color: var(--kh-muted);
  font-size: 1.05rem;
  line-height: 1.7;
}

html:has([data-knit-home-marker]) .kh-start-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.25fr) minmax(17rem, 0.75fr);
  gap: 1rem;
  align-items: stretch;
}

html:has([data-knit-home-marker]) .kh-code-panel {
  min-width: 0;
  overflow: hidden;
  border: 1px solid var(--kh-night-line);
  border-radius: 0.85rem;
  background: var(--kh-code);
  box-shadow: 0 1.5rem 4rem color-mix(in srgb, var(--kh-night) 60%, transparent);
}

html:has([data-knit-home-marker]) .kh-code-panel-head {
  display: flex;
  justify-content: space-between;
  padding: 0.75rem 1rem;
  border-bottom: 1px solid var(--kh-line);
  color: var(--kh-code-muted);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
}

html:has([data-knit-home-marker]) .kh-highlighted-code {
  margin: 0;
  overflow-x: auto;
  background: var(--kh-code);
  color: var(--kh-code-ink);
  font-size: 0.82rem;
  line-height: 1.78;
  tab-size: 2;
}

html:has([data-knit-home-marker]) .kh-highlighted-code .expressive-code,
html:has([data-knit-home-marker]) .kh-highlighted-code figure,
html:has([data-knit-home-marker]) .kh-highlighted-code pre {
  margin: 0;
  border: 0;
  border-radius: 0;
  box-shadow: none;
}

html:has([data-knit-home-marker]) .kh-highlighted-code pre {
  padding-block: 1.4rem;
  background: var(--kh-code);
}

html:not([data-theme="light"]):has([data-knit-home-marker]) .kh-highlighted-code span[style] {
  /* Keep the dark palette's token colors while the homepage is in dark mode. */
  --1: var(--0) !important;
}

html:not([data-theme="light"]):has([data-knit-home-marker]) .kh-highlighted-code span[style*="D9F5DD"] {
  /* Shiki emits double-quote delimiters separately from the string body. */
  --0: #ecc48d !important;
  --1: #ecc48d !important;
}

html:has([data-knit-home-marker]) .kh-install-code code {
  padding: 0;
  background: none;
  color: inherit;
}

html:has([data-knit-home-marker]) .kh-steps {
  display: grid;
  grid-template-rows: repeat(3, 1fr);
  gap: 0;
  margin: 0;
  padding: 0;
  border-top: 1px solid var(--kh-line);
  border-left: 1px solid var(--kh-line);
  border-radius: 0.85rem;
  overflow: hidden;
  list-style: none;
}

html:has([data-knit-home-marker]) .kh-steps li {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 1rem;
  align-content: center;
  padding: 1.35rem;
  border-right: 1px solid var(--kh-line);
  border-bottom: 1px solid var(--kh-line);
  background: var(--kh-card);
}

html:has([data-knit-home-marker]) .kh-steps li > span {
  color: var(--kh-accent);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
  font-weight: 750;
}

html:has([data-knit-home-marker]) .kh-steps li div {
  display: grid;
  gap: 0.4rem;
}

html:has([data-knit-home-marker]) .kh-steps strong {
  color: var(--kh-ink);
  font-size: 0.95rem;
}

html:has([data-knit-home-marker]) .kh-steps p {
  color: var(--kh-muted);
  font-size: 0.82rem;
  line-height: 1.55;
}

html:has([data-knit-home-marker]) .kh-steps code,
html:has([data-knit-home-marker]) .kh-feature code {
  padding: 0.08rem 0.25rem;
  border-radius: 0.25rem;
  background: color-mix(in srgb, var(--kh-accent) 14%, transparent);
  color: inherit;
  font-size: max(0.75rem, 0.88em);
}

html:has([data-knit-home-marker]) .kh-install-box {
  display: grid;
  grid-template-columns: minmax(12rem, 0.55fr) minmax(18rem, 1fr) auto;
  align-items: center;
  gap: 1.4rem;
  margin-top: 1rem;
  padding: 1rem 1.15rem;
  border: 1px solid var(--kh-line);
  border-radius: 0.85rem;
  background: var(--kh-card);
}

html:has([data-knit-home-marker]) .kh-install-box > div:first-child {
  display: grid;
  gap: 0.2rem;
}

html:has([data-knit-home-marker]) .kh-install-box > div:first-child span {
  color: var(--kh-muted);
  font-size: 0.75rem;
  text-transform: uppercase;
}

html:has([data-knit-home-marker]) .kh-install-box > div:first-child strong {
  color: var(--kh-ink);
  font-size: 0.9rem;
}

html:has([data-knit-home-marker]) .kh-install-code {
  margin: 0;
  padding: 0.8rem 1rem;
  overflow-x: auto;
  border: 1px solid var(--kh-line);
  border-radius: 0.55rem;
  background: var(--kh-code);
  color: var(--kh-code-ink);
  font-size: 0.75rem;
  line-height: 1.65;
}

html:has([data-knit-home-marker]) .kh-install-box > a,
html:has([data-knit-home-marker]) .kh-feature > a,
html:has([data-knit-home-marker]) .kh-benchmark-links > a:not(.kh-button) {
  color: var(--kh-ink);
  font-size: 0.82rem;
  font-weight: 720;
  text-decoration: none;
}

html:has([data-knit-home-marker]) .kh-install-box > a:hover,
html:has([data-knit-home-marker]) .kh-feature > a:hover,
html:has([data-knit-home-marker]) .kh-benchmark-links > a:not(.kh-button):hover {
  color: var(--kh-accent);
}

html:has([data-knit-home-marker]) .kh-feature-grid {
  position: relative;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  column-gap: clamp(2.5rem, 7vw, 7rem);
}

/* Leave a clean opening wherever Lamby sits so the divider cannot read as a
   seam between the head and body through the transparent artwork. */
html:has([data-knit-home-marker]) .kh-feature-grid::before {
  content: "";
  position: absolute;
  top: 0;
  right: 0;
  left: 0;
  height: 1px;
  background: linear-gradient(
    90deg,
    var(--kh-line) 0
      calc(100% - var(--kh-mascot-right) - var(--kh-mascot-size) - 0.75rem),
    transparent calc(100% - var(--kh-mascot-right) - var(--kh-mascot-size) - 0.75rem)
      100%
  );
  pointer-events: none;
}

html:has([data-knit-home-marker]) .kh-feature {
  min-width: 0;
  min-height: 0;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 1rem;
  padding: clamp(2rem, 4vw, 3.5rem) 0;
  border-bottom: 1px solid var(--kh-line);
}

html:has([data-knit-home-marker]) .kh-feature--wide {
  grid-column: 1 / -1;
  max-width: 54rem;
  padding-top: clamp(2.5rem, 5vw, 4.5rem);
}

html:has([data-knit-home-marker]) .kh-feature--dark {
  border-color: var(--kh-line);
}

html:has([data-knit-home-marker]) .kh-feature--dark h3,
html:has([data-knit-home-marker]) .kh-feature--dark > a {
  color: var(--kh-ink);
}

html:has([data-knit-home-marker]) .kh-feature--dark > p:not(.kh-feature-label) {
  color: var(--kh-muted);
}

html:has([data-knit-home-marker]) .kh-feature-icon {
  color: var(--kh-accent);
  font-family: var(--sl-font-mono);
  font-size: 1.6rem;
  line-height: 1;
}

html:has([data-knit-home-marker]) .kh-feature > p:not(.kh-feature-label) {
  color: var(--kh-muted);
  font-size: 0.95rem;
  line-height: 1.68;
}

html:has([data-knit-home-marker]) .kh-feature > a {
  margin-top: auto;
}

html:has([data-knit-home-marker]) .kh-chip-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

html:has([data-knit-home-marker]) .kh-chip-list li {
  padding: 0;
  color: var(--kh-muted);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
}

html:has([data-knit-home-marker]) .kh-chip-list li:not(:last-child)::after {
  content: "·";
  margin-left: 0.45rem;
  color: var(--kh-accent);
}

html:has([data-knit-home-marker]) .kh-boundary-dial {
  width: 100%;
  display: grid;
  grid-template-columns: auto 1fr auto 1fr auto;
  align-items: center;
  gap: 0.45rem;
  margin-block: 0.5rem;
  color: var(--kh-muted);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
}

html:has([data-knit-home-marker]) .kh-boundary-dial i {
  height: 1px;
  background: linear-gradient(90deg, var(--kh-line), var(--kh-accent));
}

html:has([data-knit-home-marker]) .kh-benchmark {
  position: relative;
  isolation: isolate;
  padding-block: clamp(5.5rem, 10vw, 8rem);
  color: var(--kh-paper);
}

html:has([data-knit-home-marker]) .kh-benchmark::before {
  content: "";
  position: absolute;
  z-index: -1;
  inset: 0 50%;
  width: 100vw;
  transform: translateX(-50%);
  background:
    radial-gradient(circle at 80% 15%, color-mix(in srgb, var(--kh-accent) 13%, transparent), transparent 24rem),
    var(--kh-night);
}

html:has([data-knit-home-marker]) .kh-benchmark-head {
  display: grid;
  grid-template-columns: 1fr minmax(18rem, 0.6fr);
  align-items: end;
  gap: 3rem;
  margin-bottom: 3rem;
}

html:has([data-knit-home-marker]) .kh-benchmark-head > div {
  display: grid;
  gap: 1rem;
}

html:has([data-knit-home-marker]) .kh-benchmark h2 {
  color: var(--kh-paper);
}

html:has([data-knit-home-marker]) .kh-benchmark-head > p {
  color: var(--kh-muted);
  font-size: 0.95rem;
  line-height: 1.7;
}

html:has([data-knit-home-marker]) .kh-metric-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  border-top: 1px solid var(--kh-line);
  border-left: 1px solid var(--kh-line);
}

html:has([data-knit-home-marker]) .kh-metric-grid article {
  min-width: 0;
  display: grid;
  gap: 1rem;
  padding: 2rem;
  border-right: 1px solid var(--kh-line);
  border-bottom: 1px solid var(--kh-line);
}

html:has([data-knit-home-marker]) .kh-metric-grid article > span {
  color: var(--kh-muted);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

html:has([data-knit-home-marker]) .kh-metric-grid strong {
  color: var(--kh-accent);
  font-size: clamp(2.7rem, 5vw, 4.7rem);
  line-height: 0.9;
  letter-spacing: -0.065em;
  font-variant-numeric: tabular-nums;
}

html:has([data-knit-home-marker]) .kh-metric-grid small {
  margin-left: 0.25rem;
  color: var(--kh-paper);
  font-size: max(0.75rem, 0.34em);
  letter-spacing: 0;
}

html:has([data-knit-home-marker]) .kh-metric-grid p {
  color: var(--kh-muted);
  font-size: 0.75rem;
}

html:has([data-knit-home-marker]) .kh-benchmark-links {
  display: flex;
  align-items: center;
  gap: 1.3rem;
  margin-top: 2rem;
}

html:has([data-knit-home-marker]) .kh-benchmark-links > a:not(.kh-button) {
  color: var(--kh-muted);
}

html:has([data-knit-home-marker]) .kh-use-grid {
  border-top: 1px solid var(--kh-line);
}

html:has([data-knit-home-marker]) .kh-use-grid > a {
  position: relative;
  display: grid;
  grid-template-columns: minmax(7rem, 0.5fr) minmax(12rem, 0.9fr) minmax(15rem, 1.25fr) auto;
  align-items: center;
  gap: 1.25rem;
  padding: 1.5rem 0;
  border-bottom: 1px solid var(--kh-line);
  text-decoration: none;
  transition: color 180ms ease;
}

html:has([data-knit-home-marker]) .kh-use-grid > a:hover strong {
  color: var(--kh-accent);
}

html:has([data-knit-home-marker]) .kh-use-grid span {
  color: var(--kh-accent);
  font-family: var(--sl-font-mono);
  font-size: 0.75rem;
  font-weight: 750;
  text-transform: uppercase;
}

html:has([data-knit-home-marker]) .kh-use-grid strong {
  color: var(--kh-ink);
  font-size: 1.35rem;
}

html:has([data-knit-home-marker]) .kh-use-grid p {
  max-width: 27rem;
  color: var(--kh-muted);
  font-size: 0.88rem;
  line-height: 1.55;
}

html:has([data-knit-home-marker]) .kh-use-grid i {
  color: var(--kh-accent);
  font-size: 1.25rem;
  font-style: normal;
  transition: transform 180ms ease;
}

html:has([data-knit-home-marker]) .kh-use-grid > a:hover i {
  transform: translateX(0.25rem);
}

html:has([data-knit-home-marker]) .kh-final {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.35rem;
  margin-bottom: 3rem;
  padding: clamp(4rem, 9vw, 7rem) 2rem;
  border: 1px solid var(--kh-night-line);
  border-radius: 1.2rem;
  overflow: hidden;
  background:
    radial-gradient(circle at 50% 0%, color-mix(in srgb, var(--kh-accent) 18%, transparent), transparent 26rem),
    var(--kh-night);
  color: var(--kh-paper);
  text-align: center;
}

html:has([data-knit-home-marker]) .kh-final h2 {
  max-width: 14ch;
  color: var(--kh-paper);
}

html:has([data-knit-home-marker]) .kh-final > p:not(.kh-kicker) {
  max-width: 36rem;
  color: var(--kh-muted);
  font-size: 1rem;
  line-height: 1.65;
}

html:has([data-knit-home-marker]) a:focus-visible,
html:has([data-knit-home-marker]) button:focus-visible {
  outline: 3px solid var(--kh-accent);
  outline-offset: 3px;
}

@media (max-width: 68rem) {
  html:has([data-knit-home-marker]) .kh-hero-grid {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  html:has([data-knit-home-marker]) .kh-hero-copy {
    max-width: 46rem;
  }

  html:has([data-knit-home-marker]) .kh-hero-visual {
    width: 100%;
  }

  html:has([data-knit-home-marker]) .kh-hero-bottom {
    align-items: stretch;
    flex-direction: column;
  }

  html:has([data-knit-home-marker]) .kh-start-grid {
    grid-template-columns: 1fr;
  }

  html:has([data-knit-home-marker]) .kh-steps {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    grid-template-rows: auto;
  }

  html:has([data-knit-home-marker]) .kh-install-box {
    grid-template-columns: 1fr 1.5fr;
  }

  html:has([data-knit-home-marker]) .kh-install-box > a {
    grid-column: 1 / -1;
  }

  html:has([data-knit-home-marker]) .kh-feature-grid {
    grid-template-columns: 1fr;
  }

  html:has([data-knit-home-marker]) .kh-feature {
    grid-column: auto;
  }

  html:has([data-knit-home-marker]) .kh-feature--wide {
    grid-column: auto;
  }
}

@media (max-width: 52rem) {
  html:has([data-knit-home-marker]) .kh-hero-grid,
  html:has([data-knit-home-marker]) .kh-proof,
  html:has([data-knit-home-marker]) .kh-section,
  html:has([data-knit-home-marker]) .kh-benchmark,
  html:has([data-knit-home-marker]) .kh-final {
    width: min(100% - 2rem, 76rem);
  }

  html:has([data-knit-home-marker]) .kh-hero-grid {
    min-height: 0;
    gap: 2rem;
    padding-block: 3rem;
  }

  html:has([data-knit-home-marker]) .kh-section {
    padding-block: 4rem;
  }

  html:has([data-knit-home-marker]) .kh-features {
    --kh-mascot-size: clamp(15rem, 58vw, 21rem);
    --kh-mascot-right: 0rem;
  }

  html:has([data-knit-home-marker]) .kh-mascot-watermark {
    top: 2.5rem;
    opacity: 0.08;
  }

  html:has([data-knit-home-marker]) .kh-section-intro {
    margin-bottom: 2rem;
  }

  html:has([data-knit-home-marker]) .kh-proof {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  html:has([data-knit-home-marker]) .kh-proof > div {
    min-height: 5rem;
  }

  html:has([data-knit-home-marker]) .kh-section-intro--split,
  html:has([data-knit-home-marker]) .kh-benchmark-head {
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }

  html:has([data-knit-home-marker]) .kh-steps,
  html:has([data-knit-home-marker]) .kh-feature-grid,
  html:has([data-knit-home-marker]) .kh-metric-grid {
    grid-template-columns: 1fr;
  }

  html:has([data-knit-home-marker]) .kh-use-grid > a {
    grid-template-columns: 1fr auto;
    gap: 0.45rem 1rem;
  }

  html:has([data-knit-home-marker]) .kh-use-grid span,
  html:has([data-knit-home-marker]) .kh-use-grid strong {
    grid-column: 1;
  }

  html:has([data-knit-home-marker]) .kh-use-grid p {
    grid-column: 1;
  }

  html:has([data-knit-home-marker]) .kh-use-grid i {
    grid-column: 2;
    grid-row: 1 / span 3;
  }

  html:has([data-knit-home-marker]) .kh-feature--wide {
    min-height: 0;
  }

  html:has([data-knit-home-marker]) .kh-metric-grid article {
    padding: 1.5rem 0;
    border-right: 1px solid var(--kh-line);
    border-bottom: 1px solid var(--kh-line);
  }

  html:has([data-knit-home-marker]) .kh-main-rule {
    width: min(100% - 2rem, 76rem);
  }
}

@media (max-width: 35rem) {
  html:has([data-knit-home-marker]) .kh-hero-grid {
    padding-block: 2.5rem 3rem;
  }

  html:has([data-knit-home-marker]) .kh-hero h1 {
    font-size: clamp(2.65rem, 13vw, 3.5rem);
    line-height: 1.05;
  }

  html:has([data-knit-home-marker]) .kh-release {
    max-width: 100%;
    flex-wrap: wrap;
  }

  html:has([data-knit-home-marker]) .kh-actions {
    width: 100%;
  }

  html:has([data-knit-home-marker]) .kh-button {
    width: 100%;
  }

  html:has([data-knit-home-marker]) .kh-hero .kh-button--cloud {
    width: 13.75rem;
    margin-inline: auto;
  }

  html:has([data-knit-home-marker]) .kh-install-line {
    grid-template-columns: auto 1fr;
  }

  html:has([data-knit-home-marker]) .kh-install-line > span:last-child {
    display: none;
  }

  html:has([data-knit-home-marker]) .kh-proof > div {
    display: grid;
    gap: 0.35rem;
    padding: 1rem;
  }

  html:has([data-knit-home-marker]) .kh-install-box {
    grid-template-columns: 1fr;
  }

  html:has([data-knit-home-marker]) .kh-install-box > a {
    grid-column: auto;
  }

  html:has([data-knit-home-marker]) .kh-benchmark-links {
    align-items: stretch;
    flex-direction: column;
    text-align: center;
  }

  html:has([data-knit-home-marker]) .kh-final {
    width: calc(100% - 1rem);
    margin-bottom: 0.5rem;
    border-radius: 0.9rem;
  }
}

@media (prefers-reduced-motion: reduce) {
  html:has([data-knit-home-marker]) *,
  html:has([data-knit-home-marker]) *::before,
  html:has([data-knit-home-marker]) *::after {
    scroll-behavior: auto !important;
    transition-duration: 0.001ms !important;
    animation-duration: 0.001ms !important;
    animation-iteration-count: 1 !important;
  }
}
`}</style>
